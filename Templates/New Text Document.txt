{% extends "base.html" %}

{% block title %}รายละเอียดคำสั่งซื้อ #{{ order.id }}{% endblock %}

{% block content %}
<div class="bg-white p-6 rounded-xl shadow-lg max-w-4xl mx-auto">
    <h1 class="text-3xl font-bold text-gray-800 mb-6 text-center">รายละเอียดคำสั่งซื้อ #{{ order.id }}</h1>

    <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-8">
        <!-- ข้อมูลลูกค้า -->
        <div>
            <h2 class="text-xl font-semibold text-gray-700 mb-4 border-b pb-2">ข้อมูลลูกค้า</h2>
            <p class="mb-2"><strong class="text-gray-600">ชื่อลูกค้า:</strong> {{ order.customer_name }}</p>
            <p class="mb-2"><strong class="text-gray-600">เบอร์โทร:</strong> {{ order.customer_phone if order.customer_phone is not none else '-' }}</p>
            <p class="mb-2"><strong class="text-gray-600">ที่อยู่:</strong> {{ order.customer_address if order.customer_address is not none else '-' }}</p>
            <a href="/edit/{{ order.customer_id }}" class="text-blue-600 hover:underline text-sm">แก้ไขข้อมูลลูกค้า</a>
        </div>

        <!-- ข้อมูลคำสั่งซื้อหลัก -->
        <div>
            <h2 class="text-xl font-semibold text-gray-700 mb-4 border-b pb-2">ข้อมูลคำสั่งซื้อ</h2>
            <p class="mb-2"><strong class="text-gray-600">วันที่สั่ง:</strong> {{ order.order_date }}</p>
            <p class="mb-2"><strong class="text-gray-600">ยอดรวมทั้งหมด:</strong> {{ "%.2f"|format(order.total_amount) }} บาท</p>

            <form method="POST" action="/orders/view/{{ order.id }}" class="mt-4">
                <div class="mb-2">
                    <label for="status" class="block text-gray-700 text-sm font-bold mb-1">สถานะ:</label>
                    <select id="status" name="status" class="form-input" onchange="this.form.submit()">
                        <option value="Pending" {% if order.status == 'Pending' %}selected{% endif %}>รอดำเนินการ</option>
                        <option value="Confirmed" {% if order.status == 'Confirmed' %}selected{% endif %}>ยืนยันแล้ว</option>
                        <option value="Shipped" {% if order.status == 'Shipped' %}selected{% endif %}>จัดส่งแล้ว</option>
                        <option value="Delivered" {% if order.status == 'Delivered' %}selected{% endif %}>จัดส่งสำเร็จ</option>
                        <option value="Cancelled" {% if order.status == 'Cancelled' %}selected{% endif %}>ยกเลิก</option>
                    </select>
                </div>
                <div class="mb-4">
                    <label for="tracking_no" class="block text-gray-700 text-sm font-bold mb-1">เลขพัสดุ:</label>
                    <input type="text" id="tracking_no" name="tracking_no" value="{{ order.tracking_no if order.tracking_no is not none else '' }}" class="form-input" onblur="this.form.submit()">
                    <p class="text-xs text-gray-500 mt-1">คลิกนอกช่องเพื่อบันทึกการเปลี่ยนแปลงเลขพัสดุ</p>
                </div>
            </form>
        </div>
    </div>

    <!-- รายการสินค้าในคำสั่งซื้อ -->
    <h2 class="text-xl font-semibold text-gray-700 mb-4 border-b pb-2">สินค้าในคำสั่งซื้อ</h2>
    {% if order.items %}
    <div class="overflow-x-auto rounded-lg shadow-sm border border-gray-200 mb-6">
        <table class="min-w-full divide-y divide-gray-200">
            <thead class="bg-gray-50">
                <tr>
                    <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                        สินค้า
                    </th>
                    <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                        ราคาเดิม
                    </th>
                    <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                        ราคาต่อหน่วย
                    </th>
                    <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                        จำนวน
                    </th>
                    <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                        ส่วนลด
                    </th>
                    <th scope="col" class="px-6 py-3 text-right text-xs font-medium text-gray-500 uppercase tracking-wider">
                        รวม
                    </th>
                </tr>
            </thead>
            <tbody class="bg-white divide-y divide-gray-200">
                {% for item in order.items %}
                <tr>
                    <td class="px-6 py-4 whitespace-nowrap text-sm font-medium text-gray-900">
                        {{ item.product_name }}
                        {% if item.product_description %}<p class="text-gray-500 text-xs">{{ item.product_description }}</p>{% endif %}
                    </td>
                    <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-800">{{ "%.2f"|format(item.product_original_price) }}</td>
                    <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-800">{{ "%.2f"|format(item.unit_price) }}</td>
                    <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-800">{{ item.quantity }}</td>
                    <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-800">{{ "%.2f"|format(item.discount) }}</td>
                    <td class="px-6 py-4 whitespace-nowrap text-right text-sm font-medium">{{ "%.2f"|format(item.item_total) }} บาท</td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
    {% else %}
    <p class="text-center text-gray-600 text-lg">ไม่พบรายการสินค้าในคำสั่งซื้อนี้</p>
    {% endif %}

    <div class="flex justify-start mt-6">
        <a href="/orders" class="btn btn-secondary">
            <svg class="w-5 h-5 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path></svg>
            กลับหน้ารายการคำสั่งซื้อ
        </a>
    </div>
</div>
{% endblock %}
